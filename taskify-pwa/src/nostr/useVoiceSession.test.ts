/**
 * useVoiceSession reducer tests
 *
 * Skipped until useVoiceSession.ts is implemented (voice feature planned).
 * Tests cover the pure reducer only — no DOM, no Web Speech API, no Gemini calls.
 */
import { describe, test } from "vitest";

describe.skip("useVoiceSession (not yet implemented)", () => {
  test("placeholder — implement when useVoiceSession.ts exists", () => {});
});
